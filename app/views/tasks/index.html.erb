<h1>Here the tasks</h1>

<ul>
  <% @tasks.each do |task|%>
    <li>
      <%= task.title %> |
      <%= link_to "Show", task_path(task) %> |
      <%= link_to "Edit", edit_task_path(task) %>

      </li>
  <% end %>
</ul>

<%= link_to "Add a new Task", new_task_path %>
